console.log("Gökhan")

setTimeout(() => {
    console.log("10 saniye sonra çalışacak") // 1 saniye sonra çalışacak
}, 1000);

setTimeout(() => {
    console.log("5 saniye sonra çalışacak") // 0.5 saniye sonra çalışacak
}, 500);

setTimeout(() => {
    console.log("1 saniye sonra çalışacak") // 0.75 saniye sonra çalışacak
}, 750);
console.log("Altay")
------------------------------------------------------------------------------------------------

/*
callback  asynchronous bir şekilde çalışır. Yani bir fonksiyonun bitmesini beklemeden diğer fonksiyon çalışır. Bu yüzden getSurname() fonksiyonu getName() fonksiyonundan önce çalışır ve ekrana "Altay" yazdırır.
 */


function getName(callback){
    setTimeout(() => {
    let name = "Gökhan"
    callback(name)
    },1000)
}

function getSurname(name, callback){
    setTimeout(() => {
        let surname = "Altay"
        callback(name, surname)
    }, 500)
}
function getAge(name, surname, callback){
    setTimeout(() => {
        let age = 30
        callback(name, surname, age)
    }, 500)
}

getName((name) => {
    getSurname(name, (name, surname) => {
        getAge(name, surname, (name, surname, age) => {
            console.log(name, surname, age);
        });
    });
});
-------------------------------------------------------------------------------

/* 
AJAX (Asynchronous JavaScript and XML) tarayıcıdan arka plandan veri almaya yarayan mekanizma....

*/
function getData(url,id) {
    if(id === null) {
        return url;
    }
    return `${url}/${id}`;
}

function getComments(url,id) {
    let Newurl = getData(url,id);
    const xhr = new XMLHttpRequest();
    xhr.addEventListener('readystatechange', () => {
        if (xhr.readyState === 4 && xhr.status === 200) {
            console.log(JSON.parse(xhr.responseText));
        }
    });
    xhr.open('GET', Newurl);
    xhr.send();
}
getComments('https://jsonplaceholder.typicode.com/posts', null);

------------------------Promise Konusu-----------------------------
/* promise */
const condition = true; // Change this to false to test rejection

const promise = new Promise((resolve, reject) => {
    if (condition) {
        resolve("Promise Başarılı!");
    } else {
        reject("Promise Başarısız !");
    }
});
console.log(promise);



let cehck = true; // Change this to false to test rejection

function myPromise() {
    return new Promise((resolve, reject) => {
        if (cehck) {
            resolve("Promise Başarılı!");
        } else {
            reject("Promise Başarısız !");
        }
    });
}

myPromise()
    .then((result) => {
        console.log(result);
    })
    .catch((error) => {
        console.log(error);
    });

function ReadStudents(url) {
    return new Promise((resolve, reject) => {
        const xhr = new XMLHttpRequest();
        try {
            xhr.addEventListener("readystatechange", () => {
                if (xhr.readyState === 4) {
                    if (xhr.status === 200) {
                        resolve(JSON.parse(xhr.responseText));
                    }
                }
            });
            xhr.open("GET", url);
            xhr.send();
        } catch (error) {
            reject(error);
        }
    });
}

ReadStudents("students.json")
    .then((data) => {
        console.log(data);
    })
    .catch((error) => {
        console.error(error);
    });
